import images from "./images";
import icons from "./icons";
import { COLORS, SIZES, FONTS, BUTTON, BUTTONTEXT, INPUT, HEADINGSTYLE } from "./theme";

const ROLE={
    MEMBER:1,
    COACH:2
}
export { images, icons, COLORS, SIZES, FONTS, BUTTON, BUTTONTEXT, INPUT, HEADINGSTYLE,ROLE };